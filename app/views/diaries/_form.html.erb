<%= form_with(model: diary, local: true) do |form| %>
  <% if diary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(diary.errors.count, "error") %> prohibited this diary from being saved:</h2>

      <ul>
      <% diary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :feeling %>
    <%= form.text_field :feeling, id: :diary_feeling %>
  </div>

  <div class="field">
    <%= form.label :weather %>
    <%= form.text_field :weather, id: :diary_weather %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_field :note, id: :diary_note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
